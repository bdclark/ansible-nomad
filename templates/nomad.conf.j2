description "Nomad"

start on (filesystem and net-device-up IFACE=lo)
stop on runlevel [!2345]

respawn
respawn limit 10 5
env GOMAXPROCS={{ ansible_processor_vcpus }}
setuid {{ nomad_user }}
exec {{ nomad_bin_dir }}/nomad agent -config {{ nomad_conf_dir }}
